# Índices — Registro

Reglas del proyecto (skill `database-indexes`). Cada índice creado en producción se registra aquí. Sin registro → no se crea.

## Registro

| Index                                               | Tabla                                 | Columnas                          | Tipo                                                             | Motivo                                                                                                                                                                             | Query que beneficia                                                        | Impacto esperado                                    | Costo potencial                                                   |
| --------------------------------------------------- | ------------------------------------- | --------------------------------- | ---------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- | --------------------------------------------------- | ----------------------------------------------------------------- |
| `ix_activity_logs_occurred_at`                      | `audit.activity_logs`                 | `occurred_at`                     | B-tree                                                           | Filtros por rango temporal de actividad (reportería/consultas de auditoría)                                                                                                        | `WHERE occurred_at BETWEEN ...`                                            | Index scan en vez de seq scan sobre tabla creciente | Coste en cada INSERT del trigger (índice extra por fila auditada) |
| `ix_activity_logs_table_record`                     | `audit.activity_logs`                 | `(table_name, record_id)`         | B-tree compuesto (igualdad primero)                              | Trazabilidad de una fila concreta: "historial de la fila X de la tabla Y"                                                                                                          | `WHERE table_name = ? AND record_id = ? ORDER BY occurred_at`              | Lookup puntual sin seq scan                         | Coste en cada INSERT del trigger                                  |
| `ix_allergens_name`                                 | `app.allergens`                       | `name`                            | UNIQUE (constraint)                                              | Unicidad del alergeno del catálogo                                                                                                                                                 | `WHERE name = ?` (get-or-create)                                           | Lookup puntual por nombre                           | Coste de índice en inserts ocasionales del catálogo               |
| `ix_icd10_codes_code`                               | `app.icd10_codes`                     | `code`                            | UNIQUE (constraint)                                              | Unicidad del código ICD-10 del catálogo                                                                                                                                            | `WHERE code = ?` (get-or-create)                                           | Lookup puntual por código                           | Ídem                                                              |
| `ix_cpt_codes_code`                                 | `app.cpt_codes`                       | `code`                            | UNIQUE (constraint)                                              | Unicidad del código CPT del catálogo (migración `AddCptCatalog`)                                                                                                                   | `WHERE code = ?` (get-or-create)                                           | Lookup puntual por código                           | Coste de índice en inserts ocasionales del catálogo               |
| `ix_cpt_codes_code_trgm`                            | `app.cpt_codes`                       | `code`                            | GIN trigram                                                      | Autocomplete del buscador CPT (patrón icd10): `ILIKE '%código%'`                                                                                                                   | `GET /api/v1/catalogs/cpt-codes?search=`                                   | Index scan GIN sobre el patrón                      | Escritura mayor por insert del seed                               |
| `ix_cpt_codes_description_trgm`                     | `app.cpt_codes`                       | `description`                     | GIN trigram                                                      | Búsqueda por descripción (ILIKE) en el autocomplete CPT                                                                                                                            | `ILIKE '%texto%'` sobre description                                        | Index scan GIN                                      | Ídem                                                              |
| `ix_medications_name`                               | `app.medications`                     | `name`                            | UNIQUE (constraint)                                              | Unicidad del fármaco del catálogo                                                                                                                                                  | `WHERE name = ?` (get-or-create)                                           | Lookup puntual por nombre                           | Ídem                                                              |
| `ix_patient_diagnoses_icd10_code_id`                | `app.patient_diagnoses`               | `icd10_code_id`                   | B-tree                                                           | FK a catálogo (PG no indexa FKs solo)                                                                                                                                              | JOIN agregado `patient_diagnoses → icd10_codes`                            | Index scan en JOIN                                  | Coste de escritura por fila de diagnóstico                        |
| `ix_patient_medications_medication_id`              | `app.patient_medications`             | `medication_id`                   | B-tree                                                           | FK a catálogo                                                                                                                                                                      | JOIN agregado `patient_medications → medications`                          | Index scan en JOIN                                  | Coste de escritura por fila de medicamento                        |
| `IX_patient_allergies_allergen_id`                  | `app.patient_allergies`               | `allergen_id`                     | B-tree                                                           | FK a catálogo                                                                                                                                                                      | JOIN agregado `patient_allergies → allergens`                              | Index scan en JOIN                                  | Coste de escritura por fila de alergia                            |
| `ix_patient_allergies_patient_allergen`             | `app.patient_allergies`               | `(patient_id, allergen_id)`       | UNIQUE (constraint)                                              | Un paciente no repite el mismo alergeno                                                                                                                                            | `WHERE patient_id = ? AND allergen_id = ?`                                 | Lookup puntual                                      | Coste de escritura por alergia                                    |
| `ix_unit_of_measures_code`                          | `app.unit_of_measures`                | `code`                            | UNIQUE (constraint)                                              | Unicidad de la unidad del catálogo de mediciones                                                                                                                                   | `WHERE code = ?` (get-or-create del seeder)                                | Lookup puntual por código                           | Coste de índice en inserts ocasionales del catálogo               |
| `ix_measurement_metrics_code`                       | `app.measurement_metrics`             | `code`                            | UNIQUE (constraint)                                              | Unicidad de la métrica clínica del catálogo                                                                                                                                        | `WHERE code = ?` (get-or-create del seeder)                                | Lookup puntual por código                           | Ídem                                                              |
| `ix_measurement_metrics_default_unit_id`            | `app.measurement_metrics`             | `default_unit_id`                 | B-tree                                                           | FK a catálogo (PG no indexa FKs solo)                                                                                                                                              | JOIN agregado `measurement_metrics → unit_of_measures`                     | Index scan en JOIN                                  | Coste de escritura por métrica                                    |
| `ix_measurement_reference_ranges_metric_id`         | `app.measurement_reference_ranges`    | `metric_id`                       | B-tree                                                           | FK a catálogo; lookup de rangos de una métrica                                                                                                                                     | `WHERE metric_id = ?` (resolución de rango por edad/género)                | Lookup puntual por métrica                          | Coste de escritura por rango                                      |
| `ix_measurement_reference_ranges_unit_id`           | `app.measurement_reference_ranges`    | `unit_id`                         | B-tree                                                           | FK a catálogo                                                                                                                                                                      | JOIN agregado `measurement_reference_ranges → unit_of_measures`            | Index scan en JOIN                                  | Coste de escritura por rango                                      |
| `ix_encounters_patient_id`                          | `app.encounters`                      | `patient_id`                      | B-tree                                                           | FK; historial de consultas de un paciente                                                                                                                                          | `WHERE patient_id = ? ORDER BY started_at DESC`                            | Lookup puntual por paciente                         | Coste de escritura por encounter                                  |
| `ix_encounters_professional_id`                     | `app.encounters`                      | `professional_id`                 | B-tree                                                           | FK; agenda del profesional                                                                                                                                                         | `WHERE professional_id = ? AND started_at >= ?`                            | Index scan en JOIN                                  | Coste de escritura por encounter                                  |
| `ix_encounters_status`                              | `app.encounters`                      | `status`                          | B-tree                                                           | Filtro por estado de consulta                                                                                                                                                      | `WHERE status = 'in_progress'` (consultas activas)                         | Index scan sobre conjunto pequeño                   | Coste de escritura por encounter                                  |
| `ix_encounters_type`                                | `app.encounters`                      | `type`                            | B-tree                                                           | Filtro por tipo de consulta                                                                                                                                                        | `WHERE type = 'consulta_periodica'`                                        | Index scan sobre conjunto pequeño                   | Coste de escritura por encounter                                  |
| `ix_clinical_measurements_patient_id`               | `app.clinical_measurements`           | `patient_id`                      | B-tree                                                           | FK; serie temporal de un paciente                                                                                                                                                  | `WHERE patient_id = ?`                                                     | Lookup puntual por paciente                         | Coste de escritura por medición                                   |
| `ix_clinical_measurements_metric_id`                | `app.clinical_measurements`           | `metric_id`                       | B-tree                                                           | FK a catálogo; evolución de una métrica                                                                                                                                            | `WHERE metric_id = ?` (análisis por tipo de medición)                      | Index scan en JOIN                                  | Coste de escritura por medición                                   |
| `ix_clinical_measurements_encounter_id`             | `app.clinical_measurements`           | `encounter_id`                    | B-tree                                                           | FK a encounters (nullable, monitoreo autónomo)                                                                                                                                     | JOIN agregado `clinical_measurements → encounters`                         | Index scan en JOIN                                  | Coste de escritura por medición                                   |
| `ix_clinical_measurements_patient_observed`         | `app.clinical_measurements`           | `(patient_id, observed_at DESC)`  | B-tree compuesto (igualdad primero + orden descendente)          | "Último valor por métrica" y tendencias por paciente ordenadas por fecha de observación                                                                                            | `WHERE patient_id = ? AND metric_id = ? ORDER BY observed_at DESC LIMIT 1` | Lookup puntual + top-1 sin sort                     | Coste de escritura por medición (índice compuesto)                |
| `ix_health_test_instruments_code`                   | `app.health_test_instruments`         | `code`                            | UNIQUE (constraint)                                              | Unicidad del código de instrumento                                                                                                                                                 | `WHERE code = ?` (get-or-create del seed)                                  | Lookup puntual por código                           | Coste de índice en inserts ocasionales del catálogo               |
| `ix_health_test_instruments_category`               | `app.health_test_instruments`         | `category`                        | B-tree                                                           | Filtro por categoría en el ERP                                                                                                                                                     | `WHERE category = ?` (catálogo por tipo de test)                           | Index scan sobre subconjunto                        | Coste de escritura por instrumento                                |
| `ix_health_test_instruments_is_active`              | `app.health_test_instruments`         | `is_active`                       | B-tree                                                           | Catálogo de instrumentos activos                                                                                                                                                   | `WHERE is_active = true`                                                   | Index scan pequeño                                  | Coste de escritura por instrumento                                |
| `ix_health_test_versions_instrument_id`             | `app.health_test_versions`            | `instrument_id`                   | B-tree                                                           | FK; versiones de un instrumento                                                                                                                                                    | `WHERE instrument_id = ?`                                                  | Lookup puntual por instrumento                      | Coste de escritura por versión                                    |
| `ix_health_test_versions_instrument_number`         | `app.health_test_versions`            | `(instrument_id, version_number)` | UNIQUE (constraint)                                              | Un instrumento no repite número de versión                                                                                                                                         | `WHERE instrument_id = ? AND version_number = ?`                           | Lookup puntual                                      | Coste de escritura por versión                                    |
| `ix_health_test_versions_instrument_current`        | `app.health_test_versions`            | `(instrument_id, is_current)`     | B-tree compuesto                                                 | "Versión activa por defecto" al asignar                                                                                                                                            | `WHERE instrument_id = ? AND is_current = true`                            | Lookup puntual sin sort                             | Coste de escritura por versión                                    |
| `ix_health_test_versions_status`                    | `app.health_test_versions`            | `status`                          | B-tree                                                           | Filtro por estado (draft/active/retired)                                                                                                                                           | `WHERE status = 'active'`                                                  | Index scan pequeño                                  | Coste de escritura por versión                                    |
| `ix_health_test_questions_version_id`               | `app.health_test_questions`           | `version_id`                      | B-tree                                                           | FK; preguntas de una versión                                                                                                                                                       | `WHERE version_id = ? ORDER BY sort_order`                                 | Lookup puntual por versión                          | Coste de escritura por pregunta                                   |
| `ix_health_test_questions_version_code`             | `app.health_test_questions`           | `(version_id, code)`              | UNIQUE (constraint)                                              | Código estable de pregunta dentro de una versión                                                                                                                                   | `WHERE version_id = ? AND code = ?`                                        | Lookup puntual                                      | Coste de escritura por pregunta                                   |
| `ix_health_test_answer_options_question_id`         | `app.health_test_answer_options`      | `question_id`                     | B-tree                                                           | FK; opciones de una pregunta                                                                                                                                                       | `WHERE question_id = ? ORDER BY sort_order`                                | Lookup puntual por pregunta                         | Coste de escritura por opción                                     |
| `ix_health_test_score_ranges_version_id`            | `app.health_test_score_ranges`        | `version_id`                      | B-tree                                                           | FK; rangos de interpretación de una versión                                                                                                                                        | `WHERE version_id = ? AND min_value <= ? AND max_value >= ?`               | Lookup puntual por versión                          | Coste de escritura por rango                                      |
| `ix_health_test_batteries_code`                     | `app.health_test_batteries`           | `code`                            | UNIQUE (constraint)                                              | Unicidad del código de batería                                                                                                                                                     | `WHERE code = ?` (get-or-create del seed)                                  | Lookup puntual por código                           | Coste de índice en inserts ocasionales del catálogo               |
| `ix_health_test_batteries_is_active`                | `app.health_test_batteries`           | `is_active`                       | B-tree                                                           | Catálogo de baterías activas                                                                                                                                                       | `WHERE is_active = true`                                                   | Index scan pequeño                                  | Coste de escritura por batería                                    |
| `ix_health_test_battery_items_battery_id`           | `app.health_test_battery_items`       | `battery_id`                      | B-tree                                                           | FK; ítems de una batería                                                                                                                                                           | `WHERE battery_id = ? ORDER BY sort_order`                                 | Lookup puntual por batería                          | Coste de escritura por ítem                                       |
| `ix_health_test_battery_items_instrument_id`        | `app.health_test_battery_items`       | `instrument_id`                   | B-tree                                                           | FK a instrumento                                                                                                                                                                   | JOIN `battery_items → instruments`                                         | Index scan en JOIN                                  | Coste de escritura por ítem                                       |
| `ix_health_test_battery_items_battery_instrument`   | `app.health_test_battery_items`       | `(battery_id, instrument_id)`     | B-tree compuesto                                                 | Un instrumento no se repite en una batería (revisión manual de duplicados)                                                                                                         | `WHERE battery_id = ? AND instrument_id = ?`                               | Lookup puntual                                      | Coste de escritura por ítem                                       |
| `ix_health_test_battery_assignments_patient_id`     | `app.health_test_battery_assignments` | `patient_id`                      | B-tree                                                           | FK; baterías asignadas a un paciente                                                                                                                                               | `WHERE patient_id = ?`                                                     | Lookup puntual por paciente                         | Coste de escritura por asignación                                 |
| `ix_health_test_battery_assignments_patient_status` | `app.health_test_battery_assignments` | `(patient_id, status)`            | B-tree compuesto                                                 | Baterías pendientes de un paciente (mobile)                                                                                                                                        | `WHERE patient_id = ? AND status = 'pending'`                              | Lookup puntual                                      | Coste de escritura por asignación                                 |
| `ix_health_test_battery_assignments_battery_id`     | `app.health_test_battery_assignments` | `battery_id`                      | B-tree                                                           | FK a batería                                                                                                                                                                       | JOIN agregado `battery_assignments → batteries`                            | Index scan en JOIN                                  | Coste de escritura por asignación                                 |
| `ix_health_test_assignments_patient_id`             | `app.health_test_assignments`         | `patient_id`                      | B-tree                                                           | FK; todos los tests de un paciente                                                                                                                                                 | `WHERE patient_id = ?`                                                     | Lookup puntual por paciente                         | Coste de escritura por asignación                                 |
| `ix_health_test_assignments_patient_status`         | `app.health_test_assignments`         | `(patient_id, status)`            | B-tree compuesto                                                 | "Mis tests pendientes" (mobile) y pendientes del ERP                                                                                                                               | `WHERE patient_id = ? AND status IN ('pending','in_progress')`             | Lookup puntual                                      | Coste de escritura por asignación                                 |
| `ix_health_test_assignments_version_id`             | `app.health_test_assignments`         | `version_id`                      | B-tree                                                           | FK a versión                                                                                                                                                                       | JOIN `assignments → versions`                                              | Index scan en JOIN                                  | Coste de escritura por asignación                                 |
| `ix_health_test_assignments_battery_assignment_id`  | `app.health_test_assignments`         | `battery_assignment_id`           | B-tree                                                           | FK opcional a batería asignada                                                                                                                                                     | JOIN `assignments → battery_assignments`                                   | Index scan en JOIN                                  | Coste de escritura por asignación                                 |
| `ix_health_test_assignments_status_due`             | `app.health_test_assignments`         | `(status, due_date)`              | B-tree compuesto (igualdad + rango)                              | Expiración automática de pendientes y listado por vencimiento                                                                                                                      | `WHERE status = 'pending' AND due_date < now()`                            | Index scan sobre pendientes                         | Coste de escritura por asignación                                 |
| `ix_health_test_evaluations_assignment_id`          | `app.health_test_evaluations`         | `assignment_id`                   | B-tree                                                           | FK; evaluaciones de una asignación                                                                                                                                                 | `WHERE assignment_id = ?`                                                  | Lookup puntual                                      | Coste de escritura por evaluación                                 |
| `ix_health_test_evaluations_patient_status`         | `app.health_test_evaluations`         | `(patient_id, status)`            | B-tree compuesto                                                 | Evaluaciones activas de un paciente                                                                                                                                                | `WHERE patient_id = ? AND status = 'completed'`                            | Lookup puntual                                      | Coste de escritura por evaluación                                 |
| `ix_health_test_evaluations_patient_completed`      | `app.health_test_evaluations`         | `(patient_id, completed_at)`      | B-tree compuesto (igualdad + orden)                              | Historial/evolución de un paciente ordenado por fecha                                                                                                                              | `WHERE patient_id = ? ORDER BY completed_at DESC`                          | Lookup puntual + sort desde índice                  | Coste de escritura por evaluación                                 |
| `ix_health_test_evaluations_version_id`             | `app.health_test_evaluations`         | `version_id`                      | B-tree                                                           | FK a versión; cobertura por test                                                                                                                                                   | `WHERE version_id = ?` (agregado cobertura)                                | Index scan en JOIN                                  | Coste de escritura por evaluación                                 |
| `ix_health_test_responses_evaluation_id`            | `app.health_test_responses`           | `evaluation_id`                   | B-tree                                                           | FK; respuestas de una evaluación                                                                                                                                                   | `WHERE evaluation_id = ?`                                                  | Lookup puntual                                      | Coste de escritura por respuesta                                  |
| `ix_health_test_responses_evaluation_question`      | `app.health_test_responses`           | `(evaluation_id, question_id)`    | B-tree compuesto                                                 | Una pregunta no se responde dos veces por evaluación (revisión de duplicados)                                                                                                      | `WHERE evaluation_id = ? AND question_id = ?`                              | Lookup puntual                                      | Coste de escritura por respuesta                                  |
| `ix_health_test_results_evaluation_id`              | `app.health_test_results`             | `evaluation_id`                   | B-tree                                                           | FK; resultados de una evaluación                                                                                                                                                   | `WHERE evaluation_id = ?`                                                  | Lookup puntual                                      | Coste de escritura por resultado                                  |
| `ix_health_test_results_evaluation_code`            | `app.health_test_results`             | `(evaluation_id, code)`           | B-tree compuesto                                                 | Un resultado no se duplica por código en la misma evaluación                                                                                                                       | `WHERE evaluation_id = ? AND code = ?`                                     | Lookup puntual                                      | Coste de escritura por resultado                                  |
| `ix_health_test_results_type_severity`              | `app.health_test_results`             | `(result_type, severity)`         | B-tree compuesto (migración `AddHealthTestResultsSeverityIndex`) | Agregación del dashboard por severidad (highRisk) — con 800k resultados la query sin índice hacía Parallel Seq Scan (121ms); con este índice Index Only Scan (63ms, 16x menos I/O) | `WHERE result_type = 'score' AND severity IN (...) GROUP BY severity`      | Index Only Scan paralelo                            | Coste de escritura por resultado (índice adicional)               |
| `ix_health_test_indicator_defs_code`                | `app.health_test_indicator_defs`      | `code`                            | UNIQUE (constraint)                                              | Unicidad del código de indicador                                                                                                                                                   | `WHERE code = ?`                                                           | Lookup puntual                                      | Coste de escritura por definición                                 |
| `ix_health_test_indicator_defs_is_active`           | `app.health_test_indicator_defs`      | `is_active`                       | B-tree                                                           | Indicadores activos                                                                                                                                                                | `WHERE is_active = true`                                                   | Index scan pequeño                                  | Coste de escritura por definición                                 |
| `ix_health_test_alert_rules_code`                   | `app.health_test_alert_rules`         | `code`                            | UNIQUE (constraint)                                              | Unicidad del código de regla de alerta                                                                                                                                             | `WHERE code = ?`                                                           | Lookup puntual                                      | Coste de escritura por regla                                      |
| `ix_health_test_alert_rules_is_active`              | `app.health_test_alert_rules`         | `is_active`                       | B-tree                                                           | Reglas activas a evaluar tras cada evaluación                                                                                                                                      | `WHERE is_active = true`                                                   | Index scan pequeño                                  | Coste de escritura por regla                                      |
| `ix_health_test_alerts_patient_id`                  | `app.health_test_alerts`              | `patient_id`                      | B-tree                                                           | FK; alertas de un paciente                                                                                                                                                         | `WHERE patient_id = ?`                                                     | Lookup puntual                                      | Coste de escritura por alerta                                     |
| `ix_health_test_alerts_status_severity`             | `app.health_test_alerts`              | `(status, severity)`              | B-tree compuesto                                                 | Bandeja de alertas activas ordenada por severidad (ERP)                                                                                                                            | `WHERE status = 'active' ORDER BY severity DESC`                           | Index scan sobre activas                            | Coste de escritura por alerta                                     |
| `ix_health_test_alerts_result_rule`                 | `app.health_test_alerts`              | `(result_id, rule_id)`            | B-tree compuesto                                                 | Deduplicación: una regla → a lo sumo una alerta por resultado                                                                                                                      | `WHERE result_id = ? AND rule_id = ?`                                      | Lookup puntual de dedup                             | Coste de escritura por alerta                                     |
| `ix_health_test_comments_patient_id`                | `app.health_test_comments`            | `patient_id`                      | B-tree                                                           | FK; historial de comentarios de un paciente                                                                                                                                        | `WHERE patient_id = ?`                                                     | Lookup puntual                                      | Coste de escritura por comentario                                 |
| `ix_health_test_comments_evaluation_id`             | `app.health_test_comments`            | `evaluation_id`                   | B-tree                                                           | FK; comentarios de una evaluación                                                                                                                                                  | `WHERE evaluation_id = ?`                                                  | Lookup puntual                                      | Coste de escritura por comentario                                 |
| `ix_patient_profiles_city_id`                       | `app.patient_profiles`                | `city_id`                         | B-tree (migración `AddHealthTestGeoRollups`)                     | FK geográfica sin índice (PG no indexa FKs); filtros geo del dashboard (stats por estado/ciudad, mapa, master)                                                                      | `JOIN cities/states WHERE state.code = ?` / `WHERE city_id = ?`            | Index scan en el CTE de zona del dashboard          | Coste de escritura por perfil de paciente                         |
| `ix_patient_profiles_state_id`                      | `app.patient_profiles`                | `state_id`                        | B-tree (migración `AddHealthTestGeoRollups`)                     | FK geográfica; rutas directas por estado sin pasar por cities                                                                                                                      | `WHERE state_id = ?` (consultas geo)                                       | Index scan en JOIN                                  | Coste de escritura por perfil de paciente                         |
| `ix_health_test_geo_rollups_state_code`             | `app.health_test_geo_rollups`         | `state_code`                      | B-tree (migración `AddHealthTestGeoRollups`)                     | Lecturas agrupadas por estado del rollup snapshot del mapa                                                                                                                         | `GROUP BY state_code` (derivado en memoria desde la lectura)               | Index scan parcial (rollup ~#ciudades)              | Mínimo (tabla pequeña)                                            |

_Índices de catálogos y FKs creados con la migración `NormalizeClinicalCatalogs`._
_Los índices únicos de catálogo también sirven de índice a las FK (un lookup por `name`/`code`)._
_Los compuestos `(patient_id, ...)` existentes cubren las queries por paciente._
_Índices del módulo de mediciones clínicas creados con la migración `AddClinicalMeasurements` (catálogos + encounters + clinical_measurements)._
_Índices del módulo de tests de salud creados con la migración `AddHealthTestsModule` (16 tablas en `app.`). Los compuestos `(patient_id, status)` cubren "mis tests/evaluaciones"; `(status, due_date)` cubre la expiración de pendientes; `(result_id, rule_id)` cubre la deduplicación de alertas._

_La migración `AddHealthTestGeoRollups` (2026-09-15) crea `ix_patient_profiles_city_id`/`ix_patient_profiles_state_id` y elimina los índices manuales equivalentes `IX_patient_profiles_city_id`/`IX_patient_profiles_state_id` (creados por SQL directo, fuera del modelo) para dejar un solo índice por columna bajo la convención `ix_*`. No se añadió un índice `(severity, result_type)` en `health_test_results`: sería redundante con `ix_health_test_results_type_severity` — con igualdad en ambas columnas el orden B-tree es irrelevante._

_(Registros creados con la migración `InitialAuditSchema`; se documentarán más índices conforme existan tablas de negocio y queries reales. GIN sobre `jsonb` descartado por ahora: sin queries de filtrado por contenido.)_

## Recordatorio de reglas

- FKs: índice obligatorio (PostgreSQL no lo crea solo).
- Orden de columnas: igualdad (`=`) primero, luego rango/orden.
- Tablas de alto INSERT: evaluar costo de escritura de cada índice extra.
- Validación con `EXPLAIN (ANALYZE, BUFFERS)` antes/después.
- Producción: `CREATE INDEX CONCURRENTLY` (no bloquea) — fuera de transacción (skill `migrations`).
- Evitar índices redundantes cubiertos por un compuesto.

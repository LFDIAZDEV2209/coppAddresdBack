namespace CoppAddresd.Community.Entities;

/// <summary>Visibilidad de una publicación dentro del club (null para posts del feed global).</summary>
public enum ClubPostVisibility
{
    Publico,
    Privado,
}